import { Meta, Story, Canvas } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from '@open-wc/testing';
import '../dist/yot-button.js';

<Meta title="yot-button" component="yot-button" argTypes={{ label: { control: 'text' } }}/>

# yot-button

Combination of input field + label + error handling

## How to use

### Installation

```bash
yarn add yot-button
```

```js
import 'yot-button/yot-button.js';
```

export const Template = ({ label, state }) => html`<yot-button state=${state}>${label}</yot-button>`;

<Canvas>
  <Story name="normal" args={{label: 'continue', state: 'idle'}}>
      {Template.bind({})}
  </Story>
  <Story name="loading" args={{label: 'continue', state:"loading"}}>
    {Template.bind({})}
  </Story>
  <Story name="custom loading">
    {html`<yot-button id="button2" state="loading">
      continue
      <span slot="loading">loading...</loading>
    </yot-button>`}
  </Story>
  <Story name="success" args={{label: 'continue', state:"success"}}>
    {Template.bind({})}
  </Story>
  <Story name="sizes">
{html` <yot-button size="small" id="button1">small</yot-button>
    <yot-button size="regular" id="button2">regular</yot-button>
    <yot-button size="large" id="button3">large</yot-button>
    `}
  </Story>
</Canvas>
