import { Meta, Story, Preview } from '@open-wc/demoing-storybook';
import { html } from '@open-wc/demoing-storybook';

<Meta title="Foundational/Palette" />

export const swatch = (name, color, colorOn) => html`
<li>
                    <div style="
                                color: var(--palette-${name}-on);
                                background-color: var(--palette-${name});
                                width: 100%;
                                padding: var(--inset-density-s) var(--inset-density-m);
                               ">
                                 <div style="font-family: monospace;">${color} (bg)</div>
                                 <div style="font-family: monospace;">${colorOn} (fg)</div>
                    </div>
<pre>--palette-${name}
--palette-${name}-on</pre>
                    <div>${name}</div>
                  </li>
`;

export const paletteStyles = html`<style>
                                        ul {
                                          list-style: none;
                                          display: inline-block;
                                          width: 100%;
                                        }
                                        li {
                                          margin-right: 4px;
                                          display: grid;
                                          grid-template-columns: 1fr 200px;
                                        }
                                      </style>`;

<Story name="Primary">
  {html`
    ${paletteStyles}
    <h2>Primary</h2>
    <ul>
        ${[
          { name: 'primary-light', color: '#C8E6C9', on: '#151414' },
          { name: 'primary', color: '#66BB6A', on: '#151414' },
           { name: 'primary-dark', color: '#388E3C', on: '#EEF2F1' },
        ].map(i => swatch(i.name, i.color, i.on) )}
      </ul>
  `}
</Story>

<Story name="Secondary">
  {html`
    ${paletteStyles}
     <h2>Secondary</h2>
     <ul>
        ${[
          { name: 'secondary-light', color: '#BCAAA4', on: '#151414' },
          { name: 'secondary', color: '#795548', on: '#eef2f1' },
          { name: 'secondary-dark', color: '#4E342E', on: '#eef2f1' },
        ].map(i => swatch(i.name, i.color, i.on) )}
    </ul>
  `}
</Story>

<Story name="Backgrounds">
  {html`
    ${paletteStyles}
     <h2>Backgrounds</h2>
     <ul>
        ${[
          { name: 'background-light', color: '#FAFAFA', on: '#151414' },
          { name: 'background', color: '#EEEEEE', on: '#151414' },
          { name: 'background-dark', color: '#CACACA', on: '#151414' },
        ].map(i => swatch(i.name, i.color, i.on) )}
    </ul>
  `}
</Story>

<Story name="Specialised">
  {html`
    ${paletteStyles}
    <h2>Specialised</h2>
     <ul>
        ${[
          { name: 'success', color: '#8BC34A', on: '#151414' },
          { name: 'error', color: '#D84315', on: '#eef2f1' },
          { name: 'info', color: '#FDD835', on: '#151414' },
        ].map(i => swatch(i.name, i.color, i.on) )}
    </ul>
  `}
</Story>
